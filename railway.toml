[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100

[variables]
PORT = "80"
BACKEND_URL = "http://practical-grace.railway.internal:3000"
